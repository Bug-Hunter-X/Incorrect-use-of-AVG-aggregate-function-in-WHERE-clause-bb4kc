This repository contains a common SQL error related to using aggregate functions in the WHERE clause. The provided SQL query attempts to filter employees based on a salary greater than the average salary, but the AVG function is incorrectly placed.  The solution demonstrates how to use a subquery or CTE (Common Table Expression) to correctly achieve the desired result.